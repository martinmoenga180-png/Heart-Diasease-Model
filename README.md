# Heart-Diasease-Model
The final heart disease model,trained from scratch on the kaggle dataset uses LogisticRegression after cross validation.It includes preprocessing(handling missing values,outliers,scaling,SMOTE) and predicts heart disease from patient clinical features.
